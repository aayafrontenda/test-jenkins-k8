{
  "presets": [
    ["@babel/preset-env", { "targets": { "node": "current" } }],
    [
      "@babel/preset-react",
      {
        "runtime": "automatic" // This will enable the new JSX transform
      }
    ]
  ],
  "plugins": ["@babel/plugin-syntax-jsx"]
}
